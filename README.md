# UI Autotests: Web + Mobile (Wikipedia)

## 📌 Описание проекта

Проект содержит автоматизированные UI-тесты для:
- **веб-сайта Wikipedia** (Selenium WebDriver + TestNG)
- **Android-приложения Wikipedia** (Appium + TestNG)

Цель — продемонстрировать навыки UI-автоматизации: Page Object, ожидания, стабильные проверки и воспроизводимый запуск тестов.

---

## 🧰 Стек технологий

- **Java**: JDK 11+ (у тебя можно и выше)
- **Сборка**: Maven
- **Веб**: Selenium WebDriver, TestNG, WebDriverManager
- **Мобайл**: Appium Java Client, TestNG
- **Android**: Android Emulator / реальное устройство

---

## 📁 Структура проекта

```text
src/test/java
  ├─ pages
  │   ├─ web
  │   └─ mobile
  ├─ tests
  │   ├─ web
  │   └─ mobile
  └─ utils

src/test/resources
  ├─ config.properties
  ├─ testng-mobile.xml
  └─ testng-web.xml
```
# 🧪 UI Automation Tests — Web & Mobile

Проект содержит автоматизированные UI-тесты для **веб-версии Wikipedia** (Selenium) и **Android-приложения Wikipedia** (Appium).

---

## ✅ Покрытие сценариев

### 🌐 Web (Selenium)

Тесты находятся в:

```text
src/test/java/tests/web
```

Покрываются сценарии:

- открытие главной страницы Wikipedia  
- переходы по страницам и разделам  
- поиск и проверка результатов  
- проверка открытия ключевых страниц  

---

### 📱 Mobile (Appium — Wikipedia Android)

Тесты находятся в:

```text
src/test/java/tests/mobile
```

Покрываются сценарии:

- работа поиска  
- открытие статьи  
- проверка заголовка / элементов страницы  
- дополнительные сценарии по ТЗ  

---

## 🚀 Запуск тестов

### 🌐 Запуск WEB тестов

PowerShell (Windows):

```powershell
mvn test "-DtestSuiteXmlFile=src/test/resources/testng-web.xml"
```

## 📱 Запуск MOBILE тестов

### PowerShell (Windows):

```powershell
mvn test "-DtestSuiteXmlFile=src/test/resources/testng-mobile.xml"
```

## ⚙️ Подготовка окружения для MOBILE (Appium + Android)

Ниже приведена обязательная настройка для корректного запуска мобильных тестов.

---

## 1️⃣Установить Android Studio и SDK

### 1. Установить Android Studio

### 2. Открыть SDK Manager и установить:

-Android SDK Platform (12 / 13 / 14)
-Android SDK Platform-Tools
-Android Emulator

### 3. В AVD Manager создать эмулятор
Рекомендуется: Pixel / x86_64

### 4. Запустить эмулятор

Проверить, что устройство доступно:

```bash
adb device
```
Ожидаемый результат:
```bash
emulator-5554   device
```

## 2️⃣ Установить Appium и драйвер UiAutomator2

### Установка Appium:

```bash
npm install -g appium
```

### Проверка:

```bash
appium -v
```

### Установка драйвера:

```bash
appium driver install uiautomator2
```

### Проверка установленных драйверов:

```bash
appium driver list
```
## 3️⃣ Запустить Appium Server

### Запуск: 

```bash
appium
```

### Проверка статуса (в новом окне PowerShell):

```bash
curl http://127.0.0.1:4723/status
```

Если сервер работает — вернётся JSON со статусом ready.

## 4️⃣ Установить Wikipedia APK на эмулятор

```bash
adb install "путь\к\wikipedia-alpha.apk"
```
wikipedia-alpha.apk лежит в корне проекта

### Проверить наличие пакета:

```bash
adb shell pm list packages | findstr wikipedia
```
